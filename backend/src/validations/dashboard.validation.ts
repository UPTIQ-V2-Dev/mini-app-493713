import Joi from 'joi';

const getDashboardStats = {
    // No query parameters or body needed for this endpoint
};

const getRecentActivity = {
    // No query parameters or body needed for this endpoint
};

export default {
    getDashboardStats,
    getRecentActivity
};